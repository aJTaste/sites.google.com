# AI Memory - AppHub開発履歴

## 最終更新
- 日時: 2026-02-08 21:01
- バージョン: 1.5.1
- 担当AI: Claude (Sonnet 4.5)

---

## 今回の実装 (v1.5.1)

### 実装内容
**重大なセキュリティホールの修正**

#### 1. ステルスモード中の通知問題を解決
- **問題**: ステルスモード（まなびポケット偽装）中でも通知が表示され、学校での使用時にバレるリスクがあった
- **修正**: `chat-messages.js`に`isStealthModeActive()`関数を追加し、偽装中は通知を完全にブロック
- **影響範囲**: DMとグループチャット両方

#### 2. 通知システムの改善
- グループチャットでも通知が正しく届くように修正
- 通知表示の条件を厳密化

### 変更ファイル
1. `public/sites.google.com/js/chat-messages.js` - ステルスモードチェック追加
2. `public/sites.google.com/common/updates.js` - バージョン1.5.1を追加

### 技術的詳細
```javascript
// ステルスモード状態チェック関数
function isStealthModeActive() {
  const saved = localStorage.getItem('stealthModeState');
  if (saved) {
    try {
      return JSON.parse(saved).stealthMode;
    } catch (e) {
      return false;
    }
  }
  return false;
}

// 通知表示前にチェック
if(sender && !isStealthModeActive()){ 
  showNotification(...)
}
```

---

## アプリ全体の構造理解

### 技術スタック
- **フロントエンド**: Vanilla JS (フレームワークなし)
- **バックエンド**: Supabase (PostgreSQL + Realtime)
- **認証**: Supabase Auth
- **ストレージ**: Supabase Storage
- **デプロイ**: Vercel

### 主要機能
1. **ChatHub** - リアルタイムチャット（DM + グループ）
2. **Files** - ファイル共有（最大20MB）
3. **Links** - URL共有
4. **Proxy** - フィルタリング回避ブラウザ
5. **Images** - スクショ・録画管理
6. **Docs** - 個人メモ（自動保存）
7. **Piano** - Web鍵盤
8. **Games** - Eaglercraft, しょぼんのアクション

### 特殊機能
- **ステルスモード**: Meta+1で「まなびポケット」に偽装
- **自動ステルス**: 学校時間帯に自動で偽装モードON
- **佐伊津技法**: `sites.google.com`をディレクトリ名に使用してフィルタリング回避

### ディレクトリ構造
```
public/sites.google.com/
├── common/          # 共通モジュール（core.js, stealth-mode.js等）
├── css/             # ページ別CSS
├── js/              # ページ別JS
├── assets/          # 画像・アイコン
├── games/           # ゲーム関連HTML
└── [各種HTML]       # hub.html, chat.html等
```

---

## 既知の問題

### 解決済み
- ✅ ステルスモード中の通知表示（v1.5.1で修正）
- ✅ グループチャットで通知が届かない（v1.5.0で修正）
- ✅ 入力中インジケーター未実装（v1.5.0で実装）

### 未解決（優先度順）
1. **なし（現時点で重大な問題なし）**

---

## 次回の推奨タスク

### 優先度: 高
1. **パフォーマンス最適化**
   - メッセージ読み込みの遅延ロード実装
   - 画像の遅延読み込み（Lazy Loading）
   - Supabaseクエリの最適化

2. **UX改善**
   - メッセージ検索機能
   - チャンネルのカテゴリー分け
   - メンション機能（@ユーザー名）

### 優先度: 中
3. **新機能**
   - ファイルプレビュー機能
   - リアクション絵文字
   - 音声メッセージ

4. **セキュリティ**
   - Rate Limiting（連投防止）
   - スパム対策

### 優先度: 低
5. **その他**
   - モバイルレスポンシブ改善
   - ダークモードの完全対応
   - アクセシビリティ向上

---

## 開発環境の制約

### 使用不可
- Chrome DevTools（F12）
- Chrome拡張機能
- アドレスバーでのJS実行

### 使用可能
- **Eruda**: ページに読み込むことでコンソール確認可能
- **GitHub Codespaces**: コード編集
- **Vercel**: 自動デプロイ

### フィルタリング回避技術
- **佐伊津技法**: ホワイトリストURL（youtube.com等）をフラグメントに追加
- **ディレクトリ名利用**: `sites.google.com`を常にURL内に含める

---

## コーディング規約

### スタイル
```javascript
// ✅ 良い例
const num=9;
if(condition){
  doSomething();
}

// ❌ 悪い例
const num = 9;
if ( condition ) {
  doSomething();
}
```

- **不要な空白削除**: `=`や`(`の周囲
- **インデント**: スペース2個
- **改行**: 最小限に抑える

---

## 重要な設計思想

### 1. セキュリティファースト
- 学校での使用を前提としているため、バレないことが最優先
- ステルスモードは絶対に完璧に動作しなければならない

### 2. シンプルイズベスト
- フレームワーク不使用の判断は正しい
- 軽量で高速な動作を維持

### 3. ユーザー体験
- 学生が直感的に使えるUI
- 必要な機能だけを実装（過剰な機能は避ける）

---

## 次回AIへの引き継ぎ事項

このファイルを最初に読んでください。

### すぐ確認すべきこと
1. `stealth-mode.js`の動作確認（絶対に壊さないこと）
2. 通知システムが正常に動作しているか
3. Supabaseのクエリ数が無料枠を超えていないか

### 推奨アクション
1. **まずは観察**: ユーザーフィードバックを待つ
2. **小さく改善**: 一度に大きな変更をしない
3. **テスト重視**: 特にステルスモード関連は慎重に

### 避けるべきこと
- ステルスモード機能の大幅な変更
- 既存の動作するコードの不要なリファクタリング
- フレームワークの導入

---

## メモ

### 技術的負債
- 現時点ではなし（コードベースはクリーン）

### 将来の懸念
- Supabaseの無料枠制限（ユーザー増加時）
- メッセージ量増加時のパフォーマンス
- ストレージ容量（画像・動画）

---

最終更新: 2026-02-08 21:01
次回更新者: このファイルを最初に読み、作業後に必ず更新してください。